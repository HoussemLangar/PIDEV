<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}SANTÉA - Admin Dashboard{% endblock %}</title>
    
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    {% endblock %}
</head>
<body class="admin-body">
    
    <!-- Admin Container -->
    <div class="admin-container">
        
        <!-- Sidebar Navigation -->
        <aside class="admin-sidebar">
            <div class="admin-logo">
                <i class="fas fa-heartbeat"></i>
                <span>SANTÉA</span>
            </div>
            
            <nav class="admin-nav">
                <a href="{{ path('admin_dashboard') }}" class="admin-nav-item {{ app.request.get('_route') == 'admin_dashboard' ? 'active' : '' }}">
                    <i class="fas fa-chart-line"></i>
                    <span>Dashboard</span>
                </a>
                
                <div class="nav-section-title">GESTION UTILISATEURS</div>
                <a href="{{ path('admin_users') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_users' ? 'active' : '' }}">
                    <i class="fas fa-users"></i>
                    <span>Utilisateurs</span>
                </a>
                <a href="{{ path('admin_validation') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_validation' ? 'active' : '' }}">
                    <i class="fas fa-user-check"></i>
                    <span>Validation comptes</span>
                </a>
                <a href="{{ path('admin_roles') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_roles' ? 'active' : '' }}">
                    <i class="fas fa-user-shield"></i>
                    <span>Rôles & Permissions</span>
                </a>
                
                <div class="nav-section-title">ABONNEMENTS</div>
                <a href="{{ path('admin_subscriptions') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_subscriptions' ? 'active' : '' }}">
                    <i class="fas fa-credit-card"></i>
                    <span>Abonnements</span>
                </a>
                <a href="{{ path('admin_payments') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_payments' ? 'active' : '' }}">
                    <i class="fas fa-money-bill-wave"></i>
                    <span>Paiements</span>
                </a>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-chart-pie"></i>
                    <span>Revenus</span>
                </a>
                
                <div class="nav-section-title">SERVICES MÉDICAUX</div>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-hands-helping"></i>
                    <span>Accompagnements</span>
                </a>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-calendar-check"></i>
                    <span>Rendez-vous</span>
                </a>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-file-medical"></i>
                    <span>Contrôle médical</span>
                </a>
                
                <div class="nav-section-title">PHARMACIE</div>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-clinic-medical"></i>
                    <span>Pharmacies</span>
                </a>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-pills"></i>
                    <span>Médicaments</span>
                </a>
                
                <div class="nav-section-title">TRACKER SANTÉ</div>
                <a href="{{ path('admin_analytics') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_analytics' ? 'active' : '' }}">
                    <i class="fas fa-chart-area"></i>
                    <span>Statistiques tracker</span>
                </a>
                <a href="{{ path('app_symptome_liste_index') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'app_symptome_liste' ? 'active' : '' }}">
                    <i class="fas fa-stethoscope"></i>
                    <span>Symptomes</span>
                </a>
                
                <div class="nav-section-title">CONTENUS</div>
                
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-newspaper"></i>
                    <span>Articles & Guides</span>
                </a>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-flag"></i>
                    <span>Modération</span>
                </a>
                
                <div class="nav-section-title">SYSTÈME</div>
                <a href="#" class="admin-nav-item">
                    <i class="fas fa-chart-bar"></i>
                    <span>Signalements</span>
                </a>
                <a href="{{ path('admin_settings') }}" class="admin-nav-item {{ app.request.get('_route') starts with 'admin_settings' ? 'active' : '' }}">
                    <i class="fas fa-cog"></i>
                    <span>Paramètres</span>
                </a>
            </nav>
            
            <div class="admin-user-section">
                <div class="admin-user-avatar">
                    <i class="fas fa-user-circle"></i>
                </div>
                <div class="admin-user-info">
                    <div class="admin-user-name">{{ app.user ? (app.user.fullName ?? app.user.email ?? 'Admin') : 'Admin' }}</div>
                    <div class="admin-user-role">Administrateur</div>
                </div>
                <a href="#" class="admin-logout-btn" title="Se déconnecter">
                    <i class="fas fa-sign-out-alt"></i>
                </a>
            </div>
        </aside>
        
        <!-- Main Content -->
        <main class="admin-main">
            <!-- Top Bar -->
            <div class="admin-topbar">
                <!-- admin-search supprimé -->
            </div>
            
            <!-- Content Area -->
            <div class="admin-content">
                {% block admin_content %}{% endblock %}
            </div>
        </main>
    </div>
    
    {% block javascripts %}
        <script src="{{ asset('js/admin.js') }}"></script>
    {% endblock %}
</body>
</html>
